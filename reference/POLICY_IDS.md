# Policy IDs

Suggested stable identifiers:

ERASURE.LOGICAL.SELECTIVE.V1
ERASURE.LOGICAL.TREE.V1
ERASURE.RECOVERABLE.ENCRYPTED.V1

Do not create a policy merely because an AI model requests it.

Policies are allowlisted backend definitions.
